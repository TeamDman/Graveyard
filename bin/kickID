curl -s -X POST localhost --data-binary @- << EOF
promise=
    api.removeUserFromGroup($1, $threadID, err => {
        if (err) return reject(err);
        resolve("Removed user from the group.");
    });
EOF