echo $1 >> kick.log
curl -s -X POST localhost --data-binary @- > /dev/null << EOF
promise=
    api.removeUserFromGroup($1, $threadID, err => {
        if (err) return reject(err.toString());
        resolve(0);
    });
EOF