curl -s -X POST "localhost:$PORT" --data-binary @- >> /dev/null << EOF
promise=
    api.changeAdminStatus($threadID, [$(getID $1)], ${2:-true}, err => {
        if (err) return reject(err.toString());
        resolve(0);
    });
EOF
